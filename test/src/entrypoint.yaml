apiVersion: opsler.com/v1alpha1
kind: Entrypoint
metadata:
  name: bookinfo
spec:
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - "*"
  defaultVirtualEnvironment: bookinfo